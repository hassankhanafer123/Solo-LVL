# Solo Leveling Life — Python API config.
# Reuses the same Supabase project as the Next.js app. You can copy the values
# straight from the repo root .env.local (the NEXT_PUBLIC_* ones).

SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=your-anon-or-publishable-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-or-secret-key

# Allowed CORS origins (the Next.js frontend), comma-separated.
# Dev: http://localhost:3000   Prod: https://your-app.vercel.app
CORS_ORIGINS=http://localhost:3000

# Per-client request cap (slowapi syntax). Optional; defaults to 300/minute.
RATE_LIMIT=300/minute

# Optional: only set if your Supabase project signs JWTs with HS256 (legacy).
# New projects use asymmetric ES256 keys verified via JWKS — leave this blank.
SUPABASE_JWT_SECRET=

# Email reminders (Resend). Leave RESEND_API_KEY blank to disable sending —
# the cron endpoint still runs and reports skips.
RESEND_API_KEY=
EMAIL_FROM=DayMaxing <onboarding@resend.dev>

# Absolute URL of the deployed frontend (used in email links).
APP_URL=http://localhost:3000

# Shared secret for POST /internal/cron/reminders (GitHub Actions cron).
# Generate with: openssl rand -hex 24
CRON_SECRET=

# Optional error tracking; leave blank to disable.
SENTRY_DSN=
