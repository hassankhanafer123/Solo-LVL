"""Solo Leveling Life — Python (FastAPI) backend.

Port of the Next.js server actions (app/actions/tracker.ts) and the pure
game logic in lib/. The React frontend is unchanged; this service exposes the
same behaviour over REST so the UI can call Python instead of server actions.
"""
